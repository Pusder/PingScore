<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PingScore" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/score_blue</item>
        <item name="colorSurface">@color/score_surface</item>
        <item name="android:windowBackground">@color/score_bg</item>
    </style>

    <style name="Theme.PingScore" parent="Base.Theme.PingScore" />
</resources>
